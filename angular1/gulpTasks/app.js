/**
  * Responsável por ler todo o código da aplicação construído com Angular
  */

const gulp = require('gulp')

// [Tasks que são pré-requisito] para a task (app)
gulp.task('app', ['app.html', 'app.css', 'app.js', 'app.assets'])

gulp.task('app.html', () => {

})

gulp.task('app.css', () => {

})

gulp.task('app.js', () => {

})

gulp.task('app.assets', () => {

})
